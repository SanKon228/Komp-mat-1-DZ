#include <iostream>
#include <cmath>
using namespace std;
int main() {
	int x, y, b, c, a,r;
	cout << "x= ";
	cin >> x;
	cout << "y= ";
	cin >> y;
	cout << "b= ";
	cin >> b;
	cout << "c= ";
	cin >> c;
	cout << "a= ";
	cin >> a;
	r = sqrt(pow(x,2) + pow(y,2));
	if (b <= y && y <= b + pow(c, 2)) {

	}
}